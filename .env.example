PORT=5000
# Use MongoDB Atlas if you do not want to run MongoDB locally.
# Replace <username>, <password>, and <cluster-url> with your Atlas details.
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/coinbase-clone?retryWrites=true&w=majority
JWT_SECRET=your_jwt_secret
CLIENT_URL=http://localhost:5173
